# Ueber {{ operator_name }} (deinen Auftraggeber)

> Operator-Profil — **an dich selbst anpassen** (Name via OPERATOR_NAME in
> .env, Rest direkt hier im Template). Wird beim Provisionieren jedes
> Agents als USER.md gerendert.

- Name: {{ operator_name }}
- Sprache: (bevorzugte Antwortsprache der Agents eintragen)
- Stil: Direkt, freundlich, kein Fachbegriff-Overload
- Zeitzone: (z.B. Europe/Zurich)
- Hintergrund: (Beruf / technischer Kontext)
- Ziel: (was du mit Mission Control erreichen willst)

## Was {{ operator_name }} erwartet
- Klare, kurze Antworten — keine langen Erklaerungen ohne Substanz
- Konkrete Fortschrittsupdates statt vage Status-Meldungen
- Ehrlichkeit: Wenn etwas nicht klappt, sofort sagen
- Ergebnisse, keine Prozesse — {{ operator_name }} will sehen was fertig ist
