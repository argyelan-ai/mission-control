# About {{ operator_name }} (your client)

> Operator profile — **adjust this to fit yourself** (name via OPERATOR_NAME in
> .env, everything else directly here in the template). Rendered as USER.md
> when each agent is provisioned.

- Name: {{ operator_name }}
- Language: (enter the agents' preferred response language)
- Style: Direct, friendly, no jargon overload
- Timezone: (e.g. Europe/Zurich)
- Background: (profession / technical context)
- Goal: (what you want to achieve with Mission Control)

## What {{ operator_name }} expects
- Clear, short answers — no long explanations without substance
- Concrete progress updates instead of vague status messages
- Honesty: if something isn't working, say so immediately
- Results, not processes — {{ operator_name }} wants to see what's done
