# livekit-agents 1.8.1 (released 10.09.2026) enthaelt GPTLiveModel regulaer auf
# PyPI — der frueher noetige Force-Reinstall aus dem offenen LiveKit-PR #7212
# im voice_worker/Dockerfile ist damit weg (ADR-083). Dieser Pin deckt jetzt
# alle drei livekit-Packages ab: core, plugins-openai (GPT-Live) und
# plugins-xai (Realtime-Rueckweg).
livekit-agents[openai,xai]==1.8.1
httpx>=0.27
python-dotenv>=1.0
